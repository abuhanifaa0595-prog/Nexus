
import React from 'react';

// Replaced by GestureNavigation.tsx
// Keeping empty or minimalistic component in case we want to revert or use as fallback
const MobileNavBar: React.FC = () => {
  return null;
};

export default MobileNavBar;
